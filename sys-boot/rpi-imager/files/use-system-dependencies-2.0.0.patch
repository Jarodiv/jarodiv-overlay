diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 4a24e0c6..b14c2a69 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -172,24 +172,64 @@ set(BUILD_SHARED_LIBS OFF)
 
 include(FetchContent)
 
-# Bundled liblzma
-include(dependencies/xz.cmake)
-
-# Bundled zstd
-include(dependencies/zstd.cmake)
+# System liblzma
+find_package(LibLZMA REQUIRED)
+
+# System zstd (use config package when available, fallback to pkg-config)
+find_package(ZSTD CONFIG QUIET)
+if(NOT ZSTD_FOUND)
+    find_package(PkgConfig REQUIRED)
+    pkg_check_modules(ZSTD REQUIRED libzstd)
+    set(ZSTD_INCLUDE_DIR ${ZSTD_INCLUDE_DIRS})
+    if(NOT ZSTD_INCLUDE_DIR)
+        message(FATAL_ERROR "Unable to locate zstd headers via pkg-config")
+    endif()
+endif()
 
-# Remote nghttp2
-include(dependencies/nghttp2.cmake)
+# System nghttp2 (prefer config package, fallback to pkg-config)
+find_package(NGHTTP2 CONFIG QUIET)
+if(NGHTTP2_FOUND)
+    if(TARGET nghttp2::nghttp2 AND NOT NGHTTP2_LIBRARIES)
+        set(NGHTTP2_LIBRARIES nghttp2::nghttp2)
+    endif()
+else()
+    find_package(PkgConfig REQUIRED)
+    pkg_check_modules(NGHTTP2 REQUIRED libnghttp2)
+    if(NOT NGHTTP2_LIBRARIES)
+        set(NGHTTP2_LIBRARIES ${NGHTTP2_LINK_LIBRARIES})
+    endif()
+endif()
+if(NGHTTP2_INCLUDE_DIRS)
+    set(NGHTTP2_INCLUDE_DIR ${NGHTTP2_INCLUDE_DIRS})
+endif()
 
 # Bundled yescrypt
 include(dependencies/yescrypt.cmake)
 
+# System zlib
+find_package(ZLIB REQUIRED)
+if(NOT ZLIB_INCLUDE_DIRS AND ZLIB_INCLUDE_DIR)
+    set(ZLIB_INCLUDE_DIRS ${ZLIB_INCLUDE_DIR})
+endif()
 
-# Bundled zlib
-include(dependencies/zlib.cmake)
-
-# Bundled libarchive
-include(dependencies/libarchive.cmake)
+# System libarchive
+find_package(LibArchive CONFIG QUIET)
+if(TARGET libarchive::libarchive AND NOT LibArchive_LIBRARIES)
+    set(LibArchive_LIBRARIES libarchive::libarchive)
+endif()
+if(NOT LibArchive_FOUND)
+    find_package(LibArchive REQUIRED)
+endif()
+if(TARGET libarchive::libarchive AND NOT LibArchive_INCLUDE_DIRS)
+    get_target_property(_libarchive_includes libarchive::libarchive INTERFACE_INCLUDE_DIRECTORIES)
+    if(_libarchive_includes)
+        set(LibArchive_INCLUDE_DIRS ${_libarchive_includes})
+        set(LibArchive_INCLUDE_DIR ${_libarchive_includes})
+    endif()
+endif()
+if(NOT LibArchive_INCLUDE_DIRS AND LibArchive_INCLUDE_DIR)
+    set(LibArchive_INCLUDE_DIRS ${LibArchive_INCLUDE_DIR})
+endif()
 
 # libcurl
 if(APPLE)
@@ -201,7 +241,12 @@ if(APPLE)
     set(CURL_INCLUDE_DIR ${CURL_INCLUDE_DIRS})
     
     # Note: No need to find OpenSSL on macOS - we use native Security.framework
-    # for RSA operations and CommonCrypto for hashing
+    # for RSA operations and CommonCrypto for 
+elseif(UNIX)
+    # On Linux, we can use the system libcurl
+    find_package(CURL REQUIRED)
+    # Normalize variable naming used later in include_directories
+    set(CURL_INCLUDE_DIR ${CURL_INCLUDE_DIRS})
 else()
     include(dependencies/curl.cmake)
 endif()
@@ -537,7 +582,7 @@ else()
     include(linux/PlatformPackaging.cmake)
 endif()
 
-add_dependencies(${PROJECT_NAME} zlibstatic yescrypt)
+add_dependencies(${PROJECT_NAME} yescrypt)
 include_directories(${CURL_INCLUDE_DIR} ${LibArchive_INCLUDE_DIR} ${LIBLZMA_INCLUDE_DIRS} ${LIBDRM_INCLUDE_DIRS} ${ZLIB_INCLUDE_DIRS} ${ZSTD_INCLUDE_DIR} ${YESCRYPT_INCLUDE_DIR})
 
 # Link different Qt components based on build type
